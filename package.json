{
  "name": "ai-interview-platform-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright E2E tests for the AI Interview Training Platform.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed",
    "e2e:record": "playwright test --headed --trace on"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
